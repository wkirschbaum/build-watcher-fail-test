name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Validate changelog
        run: |
          echo "Checking changelog format..."
          exit 1
